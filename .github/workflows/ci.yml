name: Run CI
permissions:
  contents: read

on:
  push:
    branches: [ main ]
    paths:
      - '*.go'
  workflow_dispatch:

jobs:
  run_ci:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: 1.18
      - run: go test
